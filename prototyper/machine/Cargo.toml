[package]
name = "rustsbi-machine"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
build = "build.rs"
description = "Bare-metal runtime foundation for RustSBI firmware"

[lib]
bench = false

[dependencies]
